<?xml version="1.0" encoding="utf-8"?>
<!--
    DUPLICATE FACE ITEM LAYOUT FOR HORIZONTAL RECYCLERVIEW
    
    PURPOSE:
    - Display individual similar face candidates
    - Show face thumbnail, name, and similarity percentage
    - Enable selection of existing person to use
    
    LAYOUT STRUCTURE:
    - Face thumbnail at top (80x80dp)
    - Person name below thumbnail
    - Similarity percentage at bottom
    - Card-based design for clear separation
    
    INTERACTION:
    - Entire card is clickable for person selection
    - Visual feedback on touch
    - Highlighted border for high confidence matches
-->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="100dp"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp"
        android:gravity="center_horizontal">

        <!-- Face Thumbnail -->
        <ImageView
            android:id="@+id/ivFace"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_marginBottom="4dp"
            android:scaleType="centerCrop"
            android:background="?attr/colorSurfaceVariant"
            android:contentDescription="Person's face" />

        <!-- Person Name -->
        <TextView
            android:id="@+id/tvName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="John Doe"
            android:textAlignment="center"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="?attr/colorOnSurface"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginBottom="2dp" />

        <!-- Similarity Percentage -->
        <TextView
            android:id="@+id/tvSimilarity"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="89% match"
            android:textAlignment="center"
            android:textSize="10sp"
            android:textColor="?attr/colorPrimary"
            android:textStyle="bold" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>